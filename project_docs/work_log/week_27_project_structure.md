Дата: 15.42 01.05.2026

1. Было решение уйти от Batch отправки к CRDT
2. Неплохо, но

  1. Нужно оставить логику "navigationStack", как 1выбор в узлах BranchSet.
  2. В "frontend/src/stores/content_manage.ts" нужно добавит activeNodeId а также сеттер и геттер.
  3. CurrentTimeline (CurrentContext) следит за activeNodeId и при его изменении начинает работу.
  4. Также у нас есть buildForward и buildBackward (оба computed). Получая команду эти две ф-ии строят   -(nodes, branchSet)[]
    - buildForward итерируется по next
    - buildBackward итерируется по prev
    - если их несколько выбирают первого
  5. makeChoice находит в текущей последовательности наш branchSet и делает следующее
    - если PreferredBranches стоит справа от activeNodeId, то мы просто меняем правую часть buildForward.
    - если PreferredBranches стоит слева, то пытается простроить от начала с выбором PreferredBranchSetChoice (тот, что запросил пользователь) до нашего текущего узла (если у него это не получается), то из всех цепочек он выбирает самую длинную и делаем в contentStore activeNodeId равным первым узлом после запршиваемого BranchSet. Если ему все таки удается добраться до нужной, то он отметает все остальные (это касается и тех случаев, когда к нему могло привести несколько путей)
  6. Т.е. для п.5 нужна новая переменная, которая будет хранить PreferredBranches по сл. правилу
    - если там уже что-то есть и нам нужно вставить еще, то мы смотрим, где он находится. Если часть справа, то их перезаписываем, а ту часть, что слева оставляем.
    - в п.5 "Если ему все таки удается добраться до нужной", то он скорее будет иметь не только запрашиваемый PreferredBranches, но и вынужденные (дабы добраться до активного узла)
    - это список объектов, где полями явяелся id branchSet и его выбор (ветка)
  
  Все ли ты понял?

3. Так. Теперь нужно решить вопрос с созданием. Но сначала надо решить несколько вопросов. У нас пока будет два флага: Manual, Generate. При выборе первого, то мы сразу вставляем его в оригинальный объект, а при выбор Generate нам нужно будет его добавить во временный объект yjs.
  - ключ: "{type}_{after_id}_{before_id}", где type=["after"|"before"|"both"], а before_id\after_id могут undefined. Т.е. всего 3 комбинации: "after_{after_id}_undefined", "before_undefined_{before_id}", "both_{after_id}_{before_id}"
  - значение: type, before_id, after_id, count, prompt.

Это ты хорошо понял?